{
  "id": "mcp-server-memory",
  "name": "Memory MCP Server",
  "version": "0.1.0",
  "description": "Memory promotion, listing, review, and management — promotes facts to wiki storage",
  "type": "mcp",
  "entrypoint": "mcp-server-memory",
  "config_schema": [
    {
      "key": "DATABASE_URL",
      "label": "Database URL",
      "type": "secret",
      "required": true
    },
    {
      "key": "DATA_DIR",
      "label": "Data directory",
      "type": "string",
      "required": true
    }
  ]
}
